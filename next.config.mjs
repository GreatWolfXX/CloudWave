/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/CloudWave',
    assetPrefix: '/CloudWave/',
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
